<div class="announce_detail_wrapper">
  <p class="announce_date">
    <%= announce.created_at.strftime('%Y/%m/%d') %>
  </p>
  <h2 class="announce_name bold"><%= announce.title %></h2>
  <div class="announce_detail">
    <%= raw(announce.contents) %>
  </div>
</div>