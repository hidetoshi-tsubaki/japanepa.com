<% content_for(:html_title) { 'Announcement Index' } %>
<% breadcrumb :announcements_index, @announce %>
<div class="announcement_index_wrapper">
  <p class="center">Announcements</p>
  <table class="new_announce_index">
    <tbody>
      <%= render partial: "announce", collection: @announcements, as: :announce %>
    </tbody>
  </table>
</div>
<div class="user_menu_in_mobile hidden">
  <%= render "user_menu_in_mobile" %>
</div>