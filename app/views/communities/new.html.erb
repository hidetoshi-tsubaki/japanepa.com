<% content_for(:html_title) { "Communitiy Form" } %>
<% breadcrumb :new_community, @community %>
<div class="form_wrapper">
  <h1 class="center">Create Community</h1>
  <%= render 'form', community: @community, action: "Create" %>
</div>