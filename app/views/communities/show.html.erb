<% content_for(:html_title) { "Community-#{@community.name}" } %>
<% breadcrumb :show_community, @community %>
<div class="section_wrapper">
  <div class="main_section">
    <div class="talk_index_wrapper" class="main_section inline">
    <h1 class="center bold page_title"><%= @community.name %></h1>
      <% if @talks.present? %>
        <%= render partial: 'talks/talk', collection: @talks, as: :talk %>
      <% else %>
        <h2 class="center" id="no_talk">no talks...</h2>
      <% end %>
    </div>
  </div>
  <div class="side_section inline community_page pc">
    <%= render "community_data" %>
    <div class="side_content">
      <h4><%= icon("fas", "tags") %> Menu (メニュー)</h4>
      <div class="side_inner_box">
        <p>
          Don't be shy! <br>
          Let's chat with members.
        </p>
        <%= link_to 'Create Talk', new_talk_path(from_community_page?: @community.id), class: 'block center create_talk_btn' %>
      </div>
    </div>
    <div class="side_content">
      <h4><%= icon("fas", "tags") %> Tags(タグ)</h4>
      <ul class="tags">
        <% if @tags.present? %>
          <%= render partial: 'tag', collection: @tags, as: :tag %>
        <% else %>
          <li>No tags....</li>
        <% end %>
      </ul>
    </div>
    <%= render "top_3_communities" %>
    <%= render "joined_communities" %>
  </div>
</div>
<div class="user_menu_in_mobile hidden">
  <%= render "user_menu_in_mobile" %>
  <%= render "community_data" %>
  <%= render "side_section" %>
</div>
<%= render "talks/create_talk_btn" %>
<%= render "back_to_top_btn" %>