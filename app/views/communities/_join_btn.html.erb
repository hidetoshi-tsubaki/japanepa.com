<% if current_user.already_joined?(community) %>
  <%= link_to 'Leave', leave_community_path(community), class: "block center join_btn", remote: true %>
<% else %>
  <%= link_to 'Join', join_community_path(community), class: "block center join_btn",  remote: true %>
<% end %>