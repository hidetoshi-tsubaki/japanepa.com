<p class="right">  
  <%= link_to admin_event_path(event), class: "delete_btn inline", data: { confirm: 'この記事を削除しますか?' }, method: :delete do %>
    <%= icon("fas", "trash-alt") %> 削除
  <% end %>
</p>
