<p class="category_new">
  <span><%= "※ ドラッグ＆ドロップで並び替え" unless title_page?(parent) %></span>
  <% if title_page?(parent) %>
    <%= link_to '新規作成', new_quiz_admin_quiz_category_path(parent), class: 'new_btn', remote: true %>
  <% elsif section_page?(parent) %>
    <%= link_to '新規作成', new_category_admin_quiz_category_path(parent), class: 'new_btn', remote: true %>
  <% else %>
    <%= link_to '新規作成', new_level_admin_quiz_categories_path, class: 'new_btn', remote: true %>
  <% end %>
</p>