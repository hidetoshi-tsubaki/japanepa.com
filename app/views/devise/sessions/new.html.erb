<% content_for(:html_title) { "Log In" } %>
<% breadcrumb :login %>
<div class="login_form_wrapper">
  <h1 class="center">~ Log In ~</h1>
  <%= render 'login_form' %>
  <p class="right">
    You don't have any Acounts?<br>
    <%= link_to "Create Acount", new_registration_path(resource_name) %>
  </p>
</div>