<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:html_title) ? yield(:html_title) : "Japanepa.com" %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= Gon::Base.render_data %>
    <%= include_gon %>
  </head>
  <body>
    <header class="<%= header_color %>">
    <%= render 'layouts/header' %>
    </header>
    <div id="flash_wrapper"><%= render "layouts/flash" %></div>
    <section id="modalArea" class="modalArea">
      <%= render 'layouts/modal' %>
    </section>
    <div id="form" class="window"></div>
    <div class="main_wrapper">
      <p><%= breadcrumbs pretext: icon("fas", "map-marker-alt"), separator: " > " %></p>
      <%= yield %>
    </div>
    <footer>
      <p>&copy; <%= Time.current.year %> Japanepa.com</p>
    </footer>
  </body>
</html>

